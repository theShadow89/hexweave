## What

One sentence on what this PR changes.

## Why

The motivation. Link the issue if any (`Closes #...`).

## How

The approach. Mention the alternatives you considered if the choice isn't obvious.

## Checks

- [ ] `npm run typecheck`
- [ ] `npm test` (integration suite)
- [ ] `npm run build`
- [ ] If geometry changed: `ORIGINAL_STL_DIR=... npm test -- original-match` passes, AND opened a generated 3MF in a slicer (Bambu Studio / PrusaSlicer) and verified the output

## Notes for the reviewer

Anything specific to look at, known gaps, follow-ups.
